#name: Auto Video
#
## Lịch: 1 Video/tuần — Thứ 4 lúc 12:00 ICT (05:00 UTC)
#
#on:
#  schedule:
#    - cron: '0 5 * * 3'
#  workflow_dispatch:
#
#jobs:
#  publish-video:
#    name: Publish Video
#    environment: GoldReels
#    runs-on: ubuntu-latest
#    timeout-minutes: 30
#
#    steps:
#      - uses: actions/checkout@v4
#
#      - uses: actions/setup-node@v4
#        with:
#          node-version: '20'
#          cache: 'npm'
#
#      - name: Install FFmpeg
#        run: sudo apt-get install -y --no-install-recommends ffmpeg fonts-noto
#
#      - name: Install dependencies
#        run: npm ci
#
#      - name: Generate Prisma client
#        run: npx prisma generate
#        env:
#          DATABASE_URL: ${{ secrets.DATABASE_URL }}
#
#      - name: Run Video pipeline
#        run: npx tsx src/index.ts --mode=video
#        env:
#          DATABASE_URL:               ${{ secrets.DATABASE_URL }}
#          AZURE_SPEECH_KEY:           ${{ secrets.AZURE_SPEECH_KEY }}
#          AZURE_SPEECH_REGION:        ${{ secrets.AZURE_SPEECH_REGION }}
#          AZURE_TTS_VOICE:            vi-VN-NamMinhNeural
#          GEMINI_API_KEY:             ${{ secrets.GEMINI_API_KEY }}
#          GEMINI_SCRIPT_MODEL:        gemini-2.5-flash
#          TTS_PROVIDER:               azure
#          FACEBOOK_PAGE_ID:           ${{ secrets.FACEBOOK_PAGE_ID }}
#          FACEBOOK_ACCESS_TOKEN:      ${{ secrets.FACEBOOK_ACCESS_TOKEN }}
#          FACEBOOK_API_VERSION:       v25.0
#          DB_AUTHOR:                  giavang24
#          ASSETS_DIR:                 ./assets
#          # Dùng /tmp — không lưu vào repo
#          TMP_DIR:                    /tmp/pipeline
#          WEB_OUTPUT_DIR:             /tmp/pipeline/output
#          WEB_UPLOAD_DIR:             /tmp/pipeline/uploads
#          LOG_LEVEL:                  info
#          VIDEO_MAX_DURATION_SECONDS: 180
#          VIDEO_WIDTH:                1920
#          VIDEO_HEIGHT:               1080
#          MAX_NEWS_PER_RUN:           1
#          STALE_LOCK_MINUTES:         30
#          WEB_PORT:                   3456
#
#      - name: Upload failure logs
#        if: failure()
#        uses: actions/upload-artifact@v4
#        with:
#          name: video-failure-${{ github.run_id }}
#          path: /tmp/pipeline/
#          retention-days: 3